#include<stdio.h>
#include<conio.h>

int isSymbol(char str)
{
    if(str=='+' || str== '-' || str== '*' || str== '/' || str== '^')
    {
        return 1;
    }

    else
    {
        return 0;
    }
}

void main()
{
    String array[400]

    char input[400];
    scanf("%s",&input);
    int len = strlen(input);

    char output[len];
    int i=0;

    while(input[i]!='\0')
    {

    }
}

